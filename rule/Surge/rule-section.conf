# DEPRECATED: this historical full-profile example is intentionally no longer
# maintained because it duplicated policy names and upstream URLs.
#
# Canonical generated RULE-SET ordering now lives at:
# rule/Surge/generated/rule-section-managed.conf
# https://raw.githubusercontent.com/mulanshan/surge/main/rule/Surge/generated/rule-section-managed.conf
#
# Copy that canonical section into the active profile, then keep device, LAN,
# personal override, GEOIP, and FINAL rules locally around it as appropriate.

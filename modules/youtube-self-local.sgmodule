#!name=YouTube Self Local
#!desc=自有可审计 YouTube 救援本地版：不修改 YouTube 响应体，优先恢复正常视频可见。
#!author=mulanshan
#!category=Enhance
#!tag=YouTube
#!openUrl=https://apps.apple.com/app/id544007664
#!arguments=启用调试模式:false
#!arguments-desc=- 调试参数：[true, false]\n\n救援本地版：暂时不运行响应脚本，不修改 browse/next/search/player/get_watch，避免正常视频内容被误删。
#!date=2026-06-07

[Rule]
# 禁用 YouTube QUIC/HTTP3，避免视频初始化请求和核心 YouTube API 走 UDP 绕过 HTTPS 处理。
AND,((DOMAIN-SUFFIX,googlevideo.com), (PROTOCOL,UDP)),REJECT
AND,((DOMAIN,youtubei.googleapis.com), (PROTOCOL,UDP)),REJECT

[Map Local]
^https?:\/\/www\.googleadservices\.com\/ data-type=text data="" status-code=204

[MITM]
hostname = %APPEND% www.googleadservices.com
